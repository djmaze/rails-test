version: '2'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/usr/src/app
    environment:
      DB_HOST: db
      DB_NAME: postgres
      DB_USER: postgres
    depends_on:
      - db

  db:
    image: postgres:9.3
